# Demo prep_roles.yaml — fictional mock cycle for Max Mustermann (see demo/CV_devops.md).
# S* ids must match ### S* blocks in your exported context.md.

mock_cycle:
  - id: S1
    label: Primary
    archetype: startup / SaaS platform
    # llm:
    #   lead_themes: EKS, ArgoCD, GitOps, Terraform
    #   language: EN
    #   framing: platform delivery at product scale
  - id: S2
    label: Stretch
    archetype: mittelstand / cloud hosting
    # llm:
    #   lead_themes: fleet ops, Ansible, customer platforms
    #   language: DE B1
    #   mock_traps_extra: on-call is current rotation — state honestly from CV dates
  - id: S3
    label: Stretch 2
    archetype: startup / platform engineering
    # llm:
    #   lead_themes: observability, Vault, CI/CD security gates
    #   language: EN

later:
  - id: S4
    label: Later
    archetype: enterprise / FinTech
    reason: brand-only — stack gaps vs CV; revisit after first mock cycle

mock_order: [S1, S2, S3]

# refine:
#   story_numbering: 1=EKS migration, 2=GitOps rollout, 3=FinOps cost reduction
#   scope_note: senior platform engineer — do not imply people management unless CV shows it
