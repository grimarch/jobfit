# Demo gap_lines.yaml — honest transfer lines for Max Mustermann (see demo/CV_devops.md).
# Skills listed here are common JD gaps; demo CV leads with GitLab CI, Terraform/Pulumi,
# Prometheus/Grafana/OpenTelemetry on AWS/GCP — not Jenkins, Datadog, or AWS CDK day-to-day.

Jenkins:
  do_not_claim: "Jenkins admin in production"
  say_instead: "Primary CI is GitLab CI and GitHub Actions; Jenkins pipeline patterns map directly"
Datadog:
  do_not_claim: "Datadog as primary observability stack"
  say_instead: "Production stack is Prometheus/Grafana/OpenTelemetry; Datadog is adjacent, not primary depth"
AWS CDK:
  do_not_claim: "Day-to-day AWS CDK ownership"
  say_instead: "IaC is Terraform and Pulumi on AWS; CDK concepts transfer, not daily CDK work"
Azure DevOps:
  do_not_claim: "Azure DevOps pipeline owner"
  say_instead: "CI/CD is GitLab CI / GitHub Actions with ArgoCD GitOps; Azure DevOps is a mapping exercise"
